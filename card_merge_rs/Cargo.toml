[package]
name = "card_merge"
version = "0.1.0"
edition = "2021"

[dependencies]
image = "0.25"
glob = "0.3"
filetime = "0.2"
# Already in the lock file via image's dependency tree; used directly here for
# localtime_r, so the merged filename's date matches the local calendar day.
libc = "0.2"
